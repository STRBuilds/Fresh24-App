// This file intentionally does nothing but re-export the real app.
// Fresh24-App.jsx remains the single source of truth, unmodified in
// structure/content/logic except the two Supabase config lines
// (converted to environment variables per deployment requirements).
export { default } from "./Fresh24-App.jsx";
